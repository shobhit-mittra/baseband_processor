# Modulation

**Files**
- `qam_16.sv`

**Description**
- Implements 16-QAM mapper (symbol mapping and normalization).
- Output: parallel I/Q fixed-point words for DAC or simulation.

**Testbench**
- `tb/tb_qam.sv`

**Notes**
- Provide sample constellation plot scripts in `/tools` or `/results`
